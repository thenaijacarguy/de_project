name: 'de_project'
version: '1.0.0'
profile: 'de_project'

model-paths: ["models"]
test-paths: ["tests"]

models:
  de_project:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts